# 装甲击打检测模块（Armor_Cmzy26_081）固件总体设计方案

版本：v1.5　|　日期：2026-08-21　|　适用硬件：SCH_Armor_Sc_EVE_081（硬件版本 080/081）　|　MCU：STM32L432KBU6（80MHz，裸机 HAL + Makefile，无 RTOS）

---

## 0. 方案基线

- **本板定位**：车队自研"车内总线"节点，**不直连官方裁判系统**。只与车队裁判核心板通过 CAN 500kbps + ISO-15765-2（ISO-TP）通信，协议见 `Driver_Examples/stm32-referee-isotp-stack/通信协议v2.md`。官方装甲模块的协议与语义仅作**功能需求参考**（第 1 章），**不追求官方协议兼容**。
- **引脚/硬件定义以 CubeMX（`Armor_Cmzy26_081.ioc`）配置为准**，原理图仅作参考（原理图个别标注有误，见 2.3）。
- **时钟前提（已确认）**：HSE=8MHz（外部振荡器；原理图标注"12MHz"有误），PLL M=1/N=20/R=2 → **SYSCLK=80MHz**（AHB/APB1/APB2 不分频），当前 `.ioc` 配置正确，无需修改。
- **通信协议栈**：采用 `Driver_Examples/stm32-referee-isotp-stack`（transport/protocol/service 层可直接拷贝，app_can.c 需为 L432 重写，见第 9 章）。**注意：该栈仓库无 LICENSE 文件、属早期代码，风险自担**（见风险 R5）。
- **文档依据**：CubeMX 工程（.ioc + 生成代码）、`Docs/ads131m04.pdf`（SBAS890D）、RM0394（STM32L4 参考手册）、RM2025 裁判系统用户手册 V1.4、AM02&AM12 / AM01&AM11 使用说明书（后三者仅取功能语义）。

---

## 1. 需求分析

### 1.1 官方装甲模块功能语义（本板功能需求来源，仅作语义参考）

| 功能       | 官方语义（来自 RM2025 手册与模块说明书）                             | 本板实现                                                |
| ---------- | -------------------------------------------------------------------- | ------------------------------------------------------- |
| 击打检测   | 检测弹丸攻击与碰撞；最高检测频率 20Hz；最小检测力 19N；最大承受 490N | 4 通道应变桥 + ADS131M04 高速采集 + 阈值判定（第 7 章） |
| 状态检测   | 上电自检；传感器损坏检测（官方：传感器损坏时红蓝交替闪烁）           | 自检 + 6 类故障诊断（第 7.5/7.6 节）                    |
| 灯驱动     | 6 种 LED 状态语义（下表）                                            | WS2812 灯带（≤13 颗）+ 板载 2 颗 LED 联动（第 8 章）   |
| 上位机通信 | 官方模块经 6 脚线缆（12V+CANH/CANL）串联到裁判系统主控               | CAN 500kbps 车内总线 + 自定义 ISO-TP 协议（第 9 章）    |
| ID 设置    | 裁判系统按拍打顺序分配装甲 ID，设置时面板 LED 慢闪                   | 核心板经协议下发 ID，写入 Flash（第 9.4 节）            |

官方装甲模块 LED 状态语义（作为本板灯光状态设计依据）：

| 官方语义            | 含义                     |
| ------------------- | ------------------------ |
| 队伍色常亮（红/蓝） | 正常工作，显示队伍身份色 |
| 快速闪烁            | 检测到击打（弹丸或碰撞） |
| 慢速闪烁            | ID 设置模式              |
| 红蓝交替            | 传感器损坏               |
| 紫色常亮            | 上电后与裁判系统通信失败 |
| 红蓝紫交替          | 模块 ID 冲突             |

### 1.2 其他需求与约束

- 供电：裁判系统 6 脚连接器提供 12V（经 0.25A 保险丝），**信号线仅 CANH/CANL 两根**，其余为电源引脚；板载多级电源（见 2.2）。**不考虑硬件负载能力约束**。
- 环境：赛场环境（振动、温漂、弹丸连发 ≤20Hz），检测需抗慢按压/扶板误报。
- 软件约束：CubeMX 生成工程（重生成会覆写生成文件），遵循修改优先生成文件原则，所有手写代码须在 `USER CODE BEGIN/END` 块或独立目录 `App/` 中。
- **保护阈值默认原则**：类似温度保护的阈值（超温、电源等）**默认取最宽松/最大值**，确保开发测试阶段保护不触发、可先验证后续功能；标定/联调后再收紧（P19/PVD，见 7.4/7.5）。

---

## 2. 硬件事实与核对清单

### 2.1 STM32 引脚映射（以 CubeMX .ioc 为准）

| 引脚            | 信号（CubeMX Label）   | 用途                                                 |
| --------------- | ---------------------- | ---------------------------------------------------- |
| PA0             | CK_IN                  | HSE 时钟源（外部 8MHz 振荡器，HSE_BYPASS）           |
| PA1/PA5/PA6/PA7 | TMP_REF1~4             | ADC1 监测 4 路 REF5025 基准 TEMP 脚（健康/超温监测） |
| PA2             | RGB_TX                 | WS2812 灯带数据（USART2 TX）                         |
| PA4             | ROHT_DAC_TEST          | DAC1_OUT1 自检注入测试点                             |
| PA9/PA10        | UART1_RX/TX            | 调试串口（115200）                                   |
| PA11/PA12       | CAN1_RX/TX             | CAN 收发器 TJA1044GTK                                |
| PA13/PA14       | SWDIO/SWCLK            | 调试口                                               |
| PA15            | ADC_CS                 | ADS131M04 片选（软件 CS，GPIO 输出）                 |
| PB0             | IND_ROHT               | 板载 LED1 =**温度过高指示**（灌电流点亮）      |
| PB1             | IND_NORM               | 板载 LED2 =**系统正常工作指示**（灌电流点亮）  |
| PB3/PB4/PB5     | SPI_ADC_SCLK/MISO/MOSI | ADS131M04 SPI                                        |
| PB6             | ADC_CLK                | **ADS131M04 CLKIN**（TIM16_CH1N，8MHz）        |
| PC14            | ADC_NDRDY              | ADS131M04 DRDY#（低有效输入，EXTI）                  |
| PC15            | ADC_SYNC               | ADS131M04 SYNC/RESET#（双功能脚，GPIO 输出）         |

### 2.2 模拟前端与电源

- **信号链**：4× 全桥应变片（连接器 U3~U6，SC_REF_OUT 2.5V 激励）→ 共模电感滤波 → 4× INA826AIDR 仪表放大器（RG=5.1kΩ 0.1%，G≈10.7）→ 10Ω 串联 + 100pF/1µF 滤波 → ADS131M04 的 AIN0~AIN3。
- **差分输入拓扑（已确认）**：**BIAS1_OUT 是 INA826 输出电压的直流偏置，接到 ADC 四个通道的 N 端**；各通道 P 端接对应放大器输出。即 ADS131M04 差分输入 = 放大器输出（P）− BIAS1_OUT 直流偏置（N）。**各通道的实际极性还受传感器接线方式影响**，固件以"极性宏 + 开机击打校对"处理（见 6.7）。
- **电源树**：12V（保险丝 0.25A）→ LMR54410 降压 5VD → HT7133 LDO 3.3VD（数字）；5VD → LM7805 5VA → LT3042 超低噪声 LDO 3.3VA（模拟，ADS131M04 AVDD 与前端）。REF5025×4 提供 2.5V 电桥激励，其 TEMP 脚接 STM32 ADC 供温度监测（超温告警源）。
- **CAN**：TJA1044GTK → 共模电感 → 裁判系统 6 脚连接器（信号线仅 CANH/CANL 两根）。
- **灯**：板载 1 颗 WS2812（GL5050RGB01H-T，DIN=PA2），DOUT 级联到外部灯带连接器 CN1（左右装甲板各 6 颗，共 13 颗，见 8.1）；板载 LED1/LED2 各串 510Ω 接 PB0/PB1（置 0 点亮）。

### 2.3 硬件核对清单（M0 阶段完成）

| #  | 核对项                             | 状态/方法                                 |
| -- | ---------------------------------- | ----------------------------------------- |
| H1 | HSE 振荡器频率                     | ✅ 已确认 8MHz（原理图"12MHz"标注有误）   |
| H2 | 各通道极性（受传感器接线影响）     | 待 M1/M2：DAC 注入 + 开机击打校对（6.7）  |
| H3 | 静态基线电压（BIAS1_OUT 实际电平） | M1 上电实测各通道基线                     |
| H4 | 裁判连接器脚位                     | ✅ 已确认：信号线仅 CANH/CANL 两根        |
| H5 | PB0/PB1 LED 语义                   | ✅ 已确认：PB0=超温指示、PB1=系统正常指示 |

---

## 3. 总体架构

### 3.1 分层结构

```
┌─────────────────────────────────────────────────────────────────────┐
│  应用层     state_machine.c（模块状态机，TIM2 1kHz 中断驱动）          │
│            led_status.c（灯光联动/动画）   main_app.c（初始化/编排）    │
├─────────────────────────────────────────────────────────────────────┤
│  功能层     detect/hit_detect.c（击打检测管线 + 强度量化 + 传感器健康）  │
│            detect/calibration.c（参数表 + 标定 + 极性校对 + Flash 持久化）│
│            comm/board_comm.c（本板消息集：HIT_EVENT/STATUS/命令处理）  │
├─────────────────────────────────────────────────────────────────────┤
│  协议层     transport/isotp.c（ISO-15765-2 分帧/重组）                │
│            protocol/{app_frame,tlv,app_ack}.c（应用帧编解码/TLV/ACK） │
│            service/{dispatcher,retry_ack_scheduler}.c（分发/重试调度）│
├─────────────────────────────────────────────────────────────────────┤
│  BSP 层     bsp/ads131m04.c（SPI+CS+DRDY+SYNC、寄存器访问、DMA 帧读） │
│            bsp/ws2812_uart.c（USART2+DMA 灯带驱动）                   │
│            bsp/temp_mon.c（ADC1 TIM1 触发式 TEMP 采集 + 窗检）        │
│            comm/app_can.c（HAL CAN 绑定：FIFO0 中断收 / 邮箱发）      │
│            comm/app_log.c（USART1 调试日志，可选）                    │
├─────────────────────────────────────────────────────────────────────┤
│  硬件层     CubeMX 生成骨架（Core/Src + HAL 驱动 + 裸机外设）          │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.2 数据流

```
上行：弹丸/碰撞 → 4×全桥应变片（差分 mV 级）
     → 4×INA826（G≈10.7）→ ADS131M04 4 通道 24bit ΔΣ（内部 VREF=1.2V）
     → SPI1 Mode1 @20MHz，6 字=18B/帧，DRDY(PC14) 下降沿触发（方案 A）
     → bsp/ads131m04.c（DMA 帧读取，极性宏取反，STATUS 字健康位检查）
     → detect/hit_detect.c（基线 EMA→高通→窗口峰值→阈值/持续判定→50ms 不应期）
     → 击打事件（通道/强度/力度/时间戳）
     → comm/board_comm.c → ISO-TP 分帧 → app_can.c
     → TJA1044GTK → CAN 500kbps → 裁判核心板（车内总线）

下行：核心板命令 → CAN FIFO0 ISR → isotp 重组 → app_frame 解码
     → dispatcher 分发 → LED 控制 / 参数配置 / ID 设置 / 自检触发
```

### 3.3 实时性关键路径

- 采集速率：fDATA = **3.906kHz**（CLKIN=8.0MHz → fMOD=4.0MHz ÷ OSR 1024），帧周期 256µs。
- 击打事件端到端时延：DRDY → EXTI 启动 DMA（<2µs）→ 帧就绪（7.2µs@20MHz）→ 检测判定（~3µs）→ 入队 → CAN 发送（邮箱空时 <100µs，繁忙时 ≤2ms）。总时延 <3ms，满足 20Hz 上报与官方语义。

---

## 4. CubeMX 配置修改清单

**工作流约束**：只改 `Armor_Cmzy26_081.ioc`（CubeMX GUI 修改后重新生成）；`Core/Src/*.c` 只允许动 `USER CODE BEGIN/END` 块；`App/` 目录不参与重新生成；Makefile 重生成后需重新追加 `-include app.mk`（见 5.2，已写入 CLAUDE.md 提醒）。

| #  | 外设      | 当前值（CubeMX 重新生成后二次核对，2026-08-21 —— **全部完成**）                                  | 目标值/状态                                                                                                                                                                                               | 说明                                                                                                                   |
| -- | --------- | --------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 1  | SPI1      | ✅ 8-bit、Prescaler=4（20MHz）、**CPHA=2EDGE（Mode 1）**、NSS 关闭 —— **全部完成**             | 无                                                                                                                                                                                                         | ADS131M04 要求 SPI Mode1（CPOL=0/CPHA=1）；SCLK≤25MHz，20MHz 留 25% 裕量；硬件 NSS 不可用于帧定界（分析见 6.4）    |
| 2  | PA15      | ✅ GPIO_Output、标签 **ADC_CS**                                                                     | 无（初始 High 由固件在 ADS 初始化前保证）                                                                                                                                                                  | 软件片选（6.4 结论）                                                                                                     |
| 3  | TIM16     | ✅ PWM（`HAL_TIM_PWM_Init`+PWM1），Period=9、Pulse=5 → 8.0MHz                                    | 无（CH1N 输出由固件 `HAL_TIMEx_PWMN_Start` 启动，置 CC1NE+MOE）                                                                                                                                        | ADS131M04 CLKIN 跑满 HR 模式（4.1）                                                                                     |
| 4  | CAN1      | ✅ Prescaler=10、BS1=13TQ、BS2=2TQ → **500kbps**                                                  | 无                                                                                                                                                                                                         | 80MHz÷10=8MHz TQ；16TQ/bit → 500kbps；采样点 (13+1)/16=87.5%                                                          |
| 5  | ADC1      | ✅ **4 通道扫描 CH6/CH10/CH11/CH12**（Rank 1-4，`ADC_SCAN_ENABLE`）+ TIM1_CC1 触发；SamplingTime=**47.5 周期**（640.5 为保守建议，TEMP 源阻抗低，47.5 足够） | 无                                                                                                                                                                                                         | 4 路 REF5025 TEMP（0.5V@25°C、1.9mV/°C）温度/超温监测；触发式采集（10.2）                                              |
| 6  | TIM1      | ✅ CH1 PWM（No Output）、TRGO=OC1 → ADC1 触发源；Period=65535                                     | 无（触发率 80M/65536≈1.22kHz，TEMP 采样足够）                                                                                                                                                              | ADC 硬件触发方案（替代原"软件触发+TIM2 节拍轮询"，合理改进，本文档已同步）                                            |
| 7  | DAC1      | ✅ OUT1（PA4）已启用（`HAL_DAC_Init`+ConfigChannel CH1；MSP 已把 PA4 配 `GPIO_MODE_ANALOG`）      | 无；注：.ioc 中 PA4 信号为 SharedAnalog、标签仍 ROHT_DAC_TEST_NC（纯外观，建议后续改名）                                                                                                                   | 自检注入点 ROHT_DAC_TEST（7.6）                                                                                         |
| 8  | USART2    | ✅ 2.6667MBd（BRR=30）、**Mode=TX-only**、PA2 推挽 AF_PP+NOPULL、**TX DMA（DMA1_Ch7）已开**      | 无                                                                                                                                                                                                         | WS2812 驱动（第 8 章）；DMA 必要性分析见 8.1                                                                           |
| 9  | USART1    | ✅ 115200 全双工                                                                                    | 无                                                                                                                                                                                                         | 调试日志（app_log.c 可选）                                                                                             |
| 10 | SPI1 DMA  | ✅ SPI1_TX（DMA1_Ch3）+ **SPI1_RX（DMA1_Ch2）**均 Normal 模式                                      | 无                                                                                                                                                                                                         | 方案 A 帧读取；L432 无 DMAMUX，固定映射：SPI1_TX=DMA1_Ch3、SPI1_RX=DMA1_Ch2、USART2_TX=DMA1_Ch7（互不冲突）            |
| 11 | TIM2      | ✅ Update interrupt @1kHz（ARR=79999）+ NVIC                                                        | 无                                                                                                                                                                                                         | 状态机/灯动画/健康评估的固定周期时基（第 10 章）                                                                       |
| 12 | NVIC      | ✅ EXTI15_10、CAN1_RX0+SCE、SPI1 全局、TIM2、USART2、**DMA1_Channel2/3/7 全开**                     | 无                                                                                                                                                                                                         | SysTick 保持默认；优先级分组 4 不变                                                                                    |
| 13 | GPIO      | ✅ PB0/PB1 输出（初始 High=灭）                                                                     | 无                                                                                                                                                                                                         | PB6 由 TIM16 接管（AF14，MspPostInit 已配置）                                                                          |
| 14 | PC14/PC15 | ✅ ADC_NDRDY 输入 / ADC_SYNC 输出                                                                   | 无                                                                                                                                                                                                         | PC14 上拉；PC15 初始 High                                                                                              |

> **CubeMX 配置已全部完成**，后续配置项仅剩固件侧 USER CODE 工作（TIM16 CH1N 启动、PA15 初始 High 等），进入 M0/M1 阶段。

### 4.1 TIM16 CLKIN 频率：8.0MHz（HR 模式，跑满）

- **决策依据（用户确认）**：ADS131M04 内部有 sinc3 数字低通滤波与可编程 OSR，**无需顾虑器件电流，时钟性能跑满**。
- 配置：TIM16 PWM 8.0MHz（ARR=9、CCR=5，50% 占空）→ CLKIN=8.0MHz → HR 模式（CLOCK.PWR=10b，恰为复位默认）→ fMOD=4.0MHz，OSR=1024（默认）→ **fDATA=3.906kHz**（帧周期 256µs）。
- 注记：8.0MHz 相对标称 8.192MHz 偏差 -2.3%（80MHz 无法整数分频出 8.192×k），fDATA 相应偏移，判定参数均按样本数/时间双标定；**M1 实测 DRDY≈3906Hz 确认**（风险 R7）。
- 如需更高数据率：OSR 可降 512/256（fDATA 7.8k/15.6k），由参数表支持运行时改（P20）。

### 4.2 时钟树

HSE=8MHz（已确认）→ PLL M=1/N=20/R=2 → SYSCLK=80MHz，AHB/APB1/APB2 不分频；PLLSAI1（N=16/R=2）→ ADC1 时钟 64MHz。现有 `.ioc` 配置正确，**无需修改时钟树**。

---

## 5. 工程结构与模块划分

### 5.1 目录策略：独立 `App/` 目录（推荐）

- CubeMX 重新生成会覆写 `Core/Src/*`（仅保留 USER CODE 块）与 Makefile；`App/` 完全不受影响。
- CubeMX 生成的 Makefile 显式列出 C_SOURCES（非通配），放 Core/Src 也不会自动编译，平铺无收益。
- `App/<层>/<模块>.c` 两级结构，按层隔离。

### 5.2 文件清单（新增 17 个 .c + 17 个 .h）

```
App/
├─ bsp/
│  ├─ ads131m04.h/.c        # ADS131M04 设备驱动：SPI/CS/DRDY/SYNC、寄存器访问、DMA 帧读、极性宏、诊断计数
│  ├─ ws2812_uart.h/.c      # USART2+DMA WS2812 驱动：3bit→字节编码表、帧缓冲、发送
│  └─ temp_mon.h/.c         # ADC1 TIM1 触发式 TEMP 采集 + 窗检 + 超温告警
├─ detect/
│  ├─ hit_detect.h/.c       # 检测管线：EMA/窗口峰值/阈值/不应期/健康诊断/极性校对
│  └─ calibration.h/.c      # 参数表（ID/默认值/范围）、Flash 持久化（双槽+CRC）、标定
├─ comm/
│  ├─ app_can.h/.c          # 重写：单 CAN1、500k、FIFO0 中断、TX 邮箱（协议栈桥接层）
│  ├─ app_log.h/.c          # USART1 调试日志（可选，自栈拷贝改 HAL）
│  ├─ board_comm.h/.c       # 本板消息集：HIT_EVENT 构造、STATUS 上报、下行命令处理
│  ├─ transport/isotp.h/.c             # 自栈拷贝（不改）
│  ├─ protocol/app_frame.h/.c, tlv.h/.c, app_ack.h/.c    # 自栈拷贝（不改）
│  └─ service/dispatcher.h/.c, retry_ack_scheduler.h/.c  # 自栈拷贝（不改）
└─ app/
   ├─ main_app.h/.c         # App_Init()/App_Loop() 编排、HAL_GetTick 时基
   ├─ state_machine.h/.c    # 状态机（TIM2 1kHz 中断驱动，第 10/11 章）
   └─ led_status.h/.c       # 官方 6 状态 → WS2812+PB0/PB1 联动、动画、亮度
```

Makefile 修改：新建 `app.mk`（内容如下），Makefile 末尾追加一行 `-include app.mk`（CubeMX 重生成会覆写 Makefile，需重新追加该行）。Makefile 的 OBJECTS 按 `notdir` 扁平化——上述 17 个文件名互不重名，安全。

```make
# ---- app.mk: 装甲模块应用层（CubeMX 重生成后需重新追加 "-include app.mk"）----
APP_SOURCES = \
App/bsp/ads131m04.c \
App/bsp/ws2812_uart.c \
App/bsp/temp_mon.c \
App/detect/hit_detect.c \
App/detect/calibration.c \
App/comm/app_can.c \
App/comm/app_log.c \
App/comm/board_comm.c \
App/comm/transport/isotp.c \
App/comm/protocol/app_frame.c \
App/comm/protocol/tlv.c \
App/comm/protocol/app_ack.c \
App/comm/service/dispatcher.c \
App/comm/service/retry_ack_scheduler.c \
App/app/state_machine.c \
App/app/led_status.c \
App/app/main_app.c
C_SOURCES += $(APP_SOURCES)
C_INCLUDES += -IApp -IApp/bsp -IApp/detect -IApp/comm \
              -IApp/comm/transport -IApp/comm/protocol -IApp/comm/service -IApp/app
```

### 5.3 对外接口（函数签名级）

```c
/* bsp/ads131m04.h */
typedef struct { uint32_t status; int32_t ch[4]; uint32_t tick_ms; } ads_frame_t;
void   ADS131M04_Init(void);                              /* 复位+全寄存器写入+回读校验，失败置全局故障 */
int    ADS131M04_ReadFrame(ads_frame_t *f);               /* 消费 DMA 就绪帧；0=OK，-1=无帧/-2=CRC 错 */
int    ADS131M04_RegWrite(uint8_t addr, uint16_t val);
int    ADS131M04_RegRead(uint8_t addr, uint16_t *val);    /* RREG 帧 + NULL 帧两拍 */
int    ADS131M04_RunSelfTest(void);                       /* 回读/DRDY/偏移/注入检查 */
void   ADS131M04_SetChannelSign(uint8_t sign_mask);       /* 通道极性宏应用：bit0-3=ch0-3，1=取反 */
void   ADS131M04_GetDiag(uint32_t *crc_err, uint32_t *drdy_cnt, uint32_t *spi_err);
void   ADS131M04_IrqDrdy(void);                           /* EXTI ISR 调用：拉低 CS、启动 TX+RX DMA */
void   ADS131M04_IrqDmaDone(void);                        /* DMA 完成 ISR 调用：拉高 CS、置帧就绪标志 */

/* detect/hit_detect.h */
void HitDetect_Init(const hit_param_t *p);
void HitDetect_Feed(const ads_frame_t *f);                /* 每帧（3.906kHz）调用 */
bool HitDetect_GetEvent(hit_event_t *e);                  /* 取最新事件（非破坏） */
void HitDetect_UpdateParams(const hit_param_t *p);
uint16_t HitDetect_GetFaultFlags(void);
int  HitDetect_CalibratePolarity(void);                   /* 预留：开机击打校对极性（6.7） */
int  HitDetect_GetWaveform(uint8_t ch, ads_sample_t *buf, uint16_t *n);  /* 波形捕获（环形窗口冻结，M2 实现） */
int  HitDetect_EstimateImpact(const hit_event_t *e, impact_info_t *info); /* 预留：弹速/入射角反推（7.8，M6 实现） */
int  HitDetect_EstimatePosition(const hit_event_t *e, pos_info_t *pos);    /* 预留：击打位置估算（7.8，M2 确定坐标轴/M6 标定） */

/* comm/app_can.h：保持协议栈原接口不变（AppCanPort/AppCanFrame/App_Can_Init/Send/SetRxCallback）*/

/* app/main_app.h */
void App_Init(void);    /* 外设→ADS→检测→灯→状态机→CAN→协议栈 依序初始化 */
void App_Loop(void);    /* 主循环一次迭代（任务表见第 10 章） */
void App_OnTick1ms(void); /* TIM2 1kHz 中断调用：状态机/灯动画/健康节拍（第 10 章） */
```

---

## 6. ADS131M04 驱动设计

### 6.1 关键接口事实（SBAS890D）与平台硬件能力（RM0394）

**ADS131M04 接口事实**：

- SPI **Mode 1**（CPOL=0/CPHA=1）；SCLK ≤25MHz（DVDD≥2.7V）；CS 跳变须在 SCLK 低电平；DIN 下降沿采样、DOUT 上升沿更新。
- 字长 16/24/32 可配（MODE.WLENGTH），**默认 24 位**；命令/寄存器响应恒为 16 位有效数据、MSB 对齐、低位补零。
- M04 每帧 **6 字** = 状态字 + 4 路数据 + CRC 字（默认关）；全双工，**响应在下一帧出现**；RESET 命令在整帧完成后才锁存。
- CLKIN 须持续提供；fMOD=fCLKIN/2，fDATA=fMOD/OSR；OSR 8 档（128~16384，默认 1024）；**内部 sinc3 数字低通滤波**。
- DRDY 低有效、默认电平式（数据可用拉低、读走整帧后拉高）；每通道 2 深 FIFO，>2 个采样周期未读会丢沿；**无 CS↔DRDY 对齐要求**，避免在新转换完成瞬间读取即可。
- 数据 24 位二进制补码，满量程差分输入 = ±1.2V/Gain（内部 VREF=1.2V）；GAIN1 每通道 1/2/4/8/16/32/64/128 八档。
- 命令：NULL=0x0000、RESET=0x0011、STANDBY=0x0022、WAKEUP=0x0033、LOCK=0x0555、UNLOCK=0x0655、RREG=0xA000|addr<<7|n、WREG=0x6000|addr<<7|n。
- POR 后等 DRDY 由低到高（接口就绪）方可通信；复位后寄存器回默认并解锁。
- **CS 定界语义**：CS 上升沿复位/重同步 SPI 接口；**支持 CS 常低连续帧**（MODE.TIMEOUT 提供失步恢复，帧须在 32768 tCLKIN=4.1ms@8MHz 内完成）；8-bit×3 分块收发可行（接口按 SCLK 边沿计数，字节边界无关）。

**STM32L432 平台能力（RM0394，决定读取方案）**：

- **无 DMAMUX**（L4+ 才有），DMA 请求固定映射：SPI1_RX=DMA1_Ch2（或 DMA2_Ch3）、SPI1_TX=DMA1_Ch3（或 DMA2_Ch4）、USART2_TX=DMA1_Ch7、TIM2_UP=DMA1_Ch2（与 SPI1_RX 同通道冲突）。
- **无 EXTI→DMA 直连**：DRDY 触发读取必经 CPU EXTI 中断启动（硬件事实）。
- SPI 数据宽 4~16bit（无 24bit 档）；TIM DMA burst（DCR/DMAR）只能写定时器自身寄存器，**不能写 SPI1_DR**；TIM2_UP 可作为普通 DMA 请求周期写 SPI1_DR。

### 6.2 寄存器表与初始化配置（全部显式写入）

完整寄存器表（SBAS890D §8.6；地址与复位默认值已核实）：

| 地址                              | 寄存器           | 类型 | 复位默认     | 本设计写入值                 | 说明                                                                                            |
| --------------------------------- | ---------------- | ---- | ------------ | ---------------------------- | ----------------------------------------------------------------------------------------------- |
| 00h                               | ID               | RO   | 24xxh（M04） | —                           | 回读校验设备型号                                                                                |
| 01h                               | STATUS           | RO   | 0500h        | —                           | 每帧状态字                                                                                      |
| 02h                               | MODE             | R/W  | 0510h        | 0510h → 再写**0410h** | 24bit 字长、CRC 关、TIMEOUT 开；**第二笔清 RESET 位以清除 STATUS.RESET 标志**             |
| 03h                               | CLOCK            | R/W  | 0F0Eh        | **0F0Eh**              | =4 通道全使能 + OSR=1024 +**PWR=HR**（默认即 HR，与 8MHz CLKIN 跑满策略一致，仍显式写入） |
| 04h                               | GAIN1            | R/W  | 0000h        | 0000h                        | 4×PGA=1（标定后按 6.6 提升）                                                                   |
| 06h                               | CFG              | R/W  | 0600h        | 0600h                        | 全局斩波关闭等默认值                                                                            |
| 07h                               | THRSHLD_MSB      | R/W  | 0000h        | 0000h                        | 阈值检测未用，显式写默认                                                                        |
| 08h                               | THRSHLD_LSB      | R/W  | 0000h        | 0000h                        | 同上（含 DCBLOCK=关）                                                                           |
| 09h/0Eh/13h/18h                   | CH0~3_CFG        | R/W  | 0000h        | 0000h                        | 相位/多路复用默认值                                                                             |
| 0Ah-Bh, 0Fh-10h, 14h-15h, 19h-1Ah | CHn_OCAL_MSB/LSB | R/W  | 0000h        | 0000h                        | 偏移校准默认                                                                                    |
| 0Ch, 11h, 16h, 1Bh                | CHn_GCAL_MSB     | R/W  | 8000h        | 8000h                        | 增益校准=1（单位增益）                                                                          |
| 0Dh, 12h, 17h, 1Ch                | CHn_GCAL_LSB     | R/W  | 0000h        | 0000h                        | 同上                                                                                            |
| 3Fh                               | RESERVED         | R/W  | 0000h        | 0000h                        | 手册要求恒写 0                                                                                  |

### 6.3 初始化序列（时序严格，全寄存器显式写入）

1. 上电延时 50ms（12V→多级 LDO 电源建立）。
2. **PC15 拉低 ≥10µs**（规格 ≥8 tCLKIN≈1µs@8MHz，取 10 倍裕量）后拉高——显式复位，寄存器回默认且解锁。
3. 等待 **DRDY 上升沿**（POR 后可通信标志），超时 100ms → 判"传感器无响应"故障。
4. 发 UNLOCK（0x0655）——复位后本就解锁，显式发送防异常状态。
5. **WREG 写入上表全部可写寄存器**（MODE、CLOCK、GAIN1、CFG、THRSHLD_MSB/LSB、CH0-3_CFG、CHn_OCAL/GCAL、3Fh=0——**即使是默认值也全部显式写一遍**）；MODE 最后追加第二笔 0410h 清 RESET 标志。
6. **RREG 回读全部寄存器逐字节校验**（每寄存器两帧），任一不符 → 上电自检失败。
7. 发 LOCK（0x0555）防运行时误写；此后改参数流程：UNLOCK → WREG → RREG 校验 → LOCK。
8. **丢弃前 2 帧**（器件响应延迟一帧，首帧为旧数据），进入正常采集。

### 6.4 帧读取方案（分析 + 结论）

#### 方案 A（已确认采用）——DRDY 外部事件触发 + SPI DMA

- 流程：PC14 DRDY 下降沿 → EXTI15_10 ISR：取**K 帧环形缓冲**的下一个空闲槽（槽满则跳过本帧并 drop 计数）→ 拉低 CS（PA15 GPIO）、启动 `HAL_SPI_TransmitReceive_DMA`（TX=静态 18B NULL 缓冲 → DMA1_Ch3；RX=写入该槽 18B ← DMA1_Ch2）→ SPI TX/RX 完成中断：拉高 CS、槽标记满 → 主循环按读指针消费帧并喂检测管线。
- 环形缓冲：默认 **K=32**（32×18B=576B，预触发历史 8.2ms）；写指针仅 ISR 侧修改、读指针仅主循环侧修改、槽状态用原子标志（风险 R17）；drop 计数纳入诊断上报（STATUS_REPORT）。
- 软件参与度：每帧 2 个轻量 ISR（共 ~2µs）@3.906kHz ≈ **0.8% CPU**；SPI 收发全程 DMA（18B@20MHz=7.2µs 后台完成）。寄存器读写利用帧间隙：暂停采集 → UNLOCK → WREG/RREG → LOCK → 恢复后丢弃 2 帧重同步。
- 为什么 L432 上 EXTI ISR 不可避免：**L432 无 DMAMUX、无 EXTI→DMA 直连**，DRDY 触发读取只能经 CPU 中断启动（RM0394 查证）。
- 优点：可靠、易调试、与寄存器读写天然兼容；DRDY 电平式天然防过读（读走整帧才拉高）。

##### 帧缓冲深度分析（双缓存是否有必要）

1. **当前击打检测需求：双缓存不必要。** 单缓冲 + 消费门控即可零撕裂——DRDY 为电平式（读走整帧才拉高）且每通道有 2 深 FIFO：消费未完成时不启动下一帧 DMA，帧序不乱、无撕裂，偶发跳帧不影响阈值判据（主循环负载 5-6%，阻塞 >256µs 概率极低）。
2. **双缓存（ping-pong）只比单缓冲多容忍 1 帧滞后**（256µs → 512µs），对"精确连续采样"帮助有限。
3. **弹速/入射角反推（7.8）真正需要的是 K 帧环形缓冲 + 触发时波形冻结**：事件窗口内零丢帧、预触发历史（冲击上升沿在阈值触发之前）、触发后衰减窗口。因此方案 A 直接采用 K=32 环形缓冲（成本 576B RAM，可忽略），一步到位满足未来需求，无需后续返工。

#### 方案 B（备选分析留存，不实现）——周期自动 SPI+DMA，零每帧 CPU

- 原理：**TIM2_UP 作为 DMA 周期请求**（L432 支持）驱动采集流水线：
  - SPI1 改 **16-bit** 数据宽；TIM2_UP @ **35.156kHz（=9×fDATA）** → DMA1_Ch2（circular，NDT=9，静态 0x0000 字）逐字周期写 SPI1_DR——每帧 9×16bit=144 SCLK，20MHz 下 7.2µs 活跃；
  - SPI1_RX → **DMA2_Ch3**（避开 Ch2 冲突）circular 36B 双缓冲，半传输/传输完成中断各报告一帧（HT/TC 天然 ping-pong）；
  - **CS 常低**（PA15 恒输出低）：ADS131M04 支持 CS 常低连续帧，帧长 256µs ≪ MODE.TIMEOUT 4.1ms，无超时风险；TIM2 与 CLKIN（TIM16）同源 80MHz，**采集速率与 fDATA 相位锁定、无漂移**。
- 软件参与度：仅 HT/TC 中断置标志（~0.1% CPU），无每帧 CS/DMA 操作。
- 限制与前提：① CS 常低按字计数定帧的行为手册未明引（M1 需实测验证，风险 R16）；② 寄存器读写需停机（暂停 DMA/TIM）→ 读写 → 重启 → 重同步；③ TIM2 被占用 → 1kHz 状态时基需改用 **TIM15**（16-bit，Prescaler=3、ARR=19999 → 1kHz）。
- 结论：作为 M1 之后的**可选零 CPU 优化**评估，不进入首版实现。

#### 硬件 NSS 分析结论：不可用，保留软件 CS

| 硬件 NSS 模式 | 行为（RM0394）                                                         | 对 ADS131M04 的影响                                                           |
| ------------- | ---------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| SSOE=1        | NSS 在 SPE=1 全程保持低，SPE=0 才拉高（§41.4.5）                      | 无帧边沿，无法定界每帧 6 字                                                   |
| NSSP=1        | 仅 CPHA=0 时有效（§41.4.12），且 8-bit 数据宽时每 8 SCLK 打一个高脉冲 | ADS 要求 CPHA=1 → NSSP 直接失效；即使可用也会在 24bit 字中间打脉冲破坏字同步 |

故**片选用 PA15 GPIO 软件控制**（CS 跳变在 SCLK 空闲低电平时进行，满足规格）。**当前 .ioc 硬件片选已由用户关闭（PA15=GPIO_Output），软件 CS 方案已就位**，仅需按第 4 章表改标签为 ADC_CS。

### 6.5 寄存器读写 API 与错误处理

- `RegRead` = RREG 帧 + NULL 帧两拍；`RegWrite` = WREG（命令字+数据字）帧 + RREG 回读校验（6.3 全部寄存器均走此路径）。
- 帧级错误：HAL/DMA 超时/参数错 → 计数并丢弃本帧；开启 CRC 后第 6 字按 CCITT-16 校验前 5 字，错则 `crc_err++` 丢弃。
- 状态字健康位每帧检查：REG_MAP 异常、CRC_ERR、RESET（意外复位）、LOCK 位与预期不符 → 置故障位。
- 诊断计数（spi_err/crc_err/drdy_cnt）纳入 STATUS_REPORT 上报与调试变量表。
- 中断归属：`ADS131M04_IrqDrdy()` 挂 EXTI15_10；`ADS131M04_IrqDmaDone()` 挂 SPI1 全局中断与 DMA 中断回调。

### 6.6 增益档位（GAIN1）

- 初始 Gain=1（满量程 ±1.2V）：灵敏度未知前保证最大头屋（估算 490N 满程时 INA826 输出 ~54mV，仅占 4.5% FS，不会饱和）。
- M2 标定后逐通道提升，目标"490N 峰值 ≈ 50~70% FS"；GAIN1 每通道 3bit 可独立配置。
- 噪声预算：ADC 本底（OSR1024/HR）~µV 级，被 INA826 输出噪声（~3.5µV RMS）主导，约 24~30 LSB@Gain=1；相对 19N 阈值（7.4）有 >60dB 裕量，**增益以满程利用率为准**。

### 6.7 通道极性处理（宏定义 + 开机校对接口）

- **极性宏**：`App/bsp/ads131m04.h` 定义 `#define ADC_CH_SIGN_MASK 0x00`——bit0-3 对应 ch0-3，**1=该通道取反**（BIAS1_OUT 接 4 通道 N 端，各通道极性最终取决于传感器接线方式）。驱动层按宏对帧数据取反（`ch[i] = -ch[i]`），上层不再感知；运行时可经 `ADS131M04_SetChannelSign(mask)` 覆盖（从 Flash 标定值恢复）。
- **开机击打校对接口（预留）**：`int HitDetect_CalibratePolarity(void)`——开机/自检时经一次**已知方向**的击打（或 DAC 注入已知符号激励），校验 4 通道响应符号，与预期不符的通道自动写入符号掩码并持久化到 Flash。接口在 M1 定义、M2 落地完整流程；在标定完成前极性以宏初始值运行。

---

## 7. 击打检测算法设计

输入：4×int32（符号已按 6.7 修正），**3.90625kHz**（dt=0.256ms）。

**四传感器融合模型**：4 个通道对应 4 个传感器，构成完整检测模块；**传感器在受击平面上中心对称分布**。弹丸击打不一定打在击打平面中心，一次击打会在 4 通道上**同时**产生响应，且**强度随击打位置分布**（离击打点近的传感器分力大）。因此检测按"单模块融合"设计：以四通道分力之和（合力）作为判定信号，**单次击打全局只产生一个事件**。

### 7.1 处理管线

```
各通道：raw → [基线 EMA：bl += (raw-bl)>>13，τ≈2.1s] → hp = raw - bl
    → 合成：hit_sum = Σhp(ch)（四通道分力之和，即合力信号）
    → |hit_sum| → [32 样本环形窗最大值 peak（8.2ms）]
    → 判定：peak > THR_HIT 且 |hit_sum| 持续 ≥4 样本（1.02ms）
    → 触发：记录事件（合力峰值/能量/时间戳 + 四通道峰值分布）→ 进入 50ms 不应期
```

- **基线漂移补偿**：每通道独立 EMA，α=2^-13（dt=0.256ms → τ≈2.1s），跟踪安装形变、温漂、缓慢挤压；整数移位实现，零除法零浮点。
- **高通**：`hp = raw − bl` 即一阶高通（截止 ~0.076Hz），配合窗口峰值剔除慢按压（扶板不误报）与直流分量。
- **通道融合**：`hit_sum = Σhp(ch)`（合力），判定、窗口峰值、持续判定均在合力信号上进行——中心对称布局下合力与击打位置无关，天然保证"一次击打=一个事件"；50ms 不应期作为防抖辅助（同一弹丸的次级振动不重复触发）。
- **窗口峰值**：32 样本环形缓冲求合力信号最大值（31 次比较/帧，~1µs），抗单样本毛刺。
- **持续判定**：合力信号超阈值连续样本数 ≥4（1.02ms）才判有效，排除窄脉冲干扰。
- **事件内容**：合力峰值 + 触发时刻**四通道各自的窗口峰值分布**（`peak_ch[4]`，反映击打位置，供 7.8 位置估算与核心板二次处理）。

### 7.2 强度量化（"力度/伤害"上报）

- 事件能量：触发后 32 样本窗内 Σ|hit_sum|（可复现、对滤波衰减不敏感）。
- **力度映射（合力模型）**：`F = K_N × Σ(peak_ch[i] × K_ch[i])`——中心对称布局下四通道分力之和与击打位置无关，力度用合力标定与上报；K_N 由 19N/100N 两点砝码标定（见 7.7），单位 0.01N；K_ch 为通道一致性归一化（P07）。
- 强度百分数：`I = clamp((F − 19N)/(490N − 19N), 0, 100)`（%），`peak_ch[4]` 一并上报，供核心板二次处理与位置反推。

### 7.3 击打事件数据结构

```c
typedef struct {
    uint8_t  ch;          /* 触发通道（合力信号判定时恒为 0xFF=融合，预留） */
    uint8_t  intensity;   /* 强度 0-100%（合力模型，7.2） */
    uint16_t force_01n;   /* 合力，0.01N 单位（7.2） */
    uint32_t peak;        /* 合力峰值绝对值 */
    uint32_t peak_ch[4];  /* 四通道峰值分布（反映击打位置，供 7.8 位置估算） */
    uint32_t ts_ms;       /* HAL_GetTick 时间戳 */
    uint32_t impulse;     /* 预留：冲量（单位经标定后定，见 7.8） */
    uint16_t duration_ms; /* 预留：撞击时间=脉宽（7.8） */
} hit_event_t;
```

### 7.4 可调参数表（初值 + 依据，采样率 3.906kHz）

| ID  | 参数                 | 初值           | 范围      | 单位         | 依据/标定                                                                                                                        |
| --- | -------------------- | -------------- | --------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------- |
| P01 | 基线时间常数 S_BL    | 13             | 8~16      | 移位位数     | τ=2^13×0.256ms≈2.1s，覆盖温漂/安装蠕变                                                                                        |
| P02 | 窗口长度             | 32             | 8~128     | 样本         | 8.2ms，覆盖弹丸接触时间                                                                                                          |
| P03 | 最小持续             | 4              | 1~64      | 样本         | 1.02ms，滤窄脉冲                                                                                                                 |
| P04 | THR_HIT              | 标定后写入     | 0~FS      | counts       | =19N×K_N×1.2（虚警裕量）；初装 20000 仅调试                                                                                    |
| P05 | 不应期               | 50             | 10~200    | ms           | 对应官方 20Hz 上限（≈195 样本）                                                                                                 |
| P06 | K_N 力度系数         | 标定           | 0.01~100  | counts/0.01N | 19N/100N 两点标定                                                                                                                |
| P07 | 通道增益补偿 K_ch[4] | 1.0            | 0.5~2.0   | -            | 桥一致性归一化                                                                                                                   |
| P08 | 饱和阈值             | 0.9FS          | 0.5~1.0   | -            | 数据手册线性区                                                                                                                   |
| P09 | 饱和判定时长         | 1000           | 100~5000  | ms           | -                                                                                                                                |
| P10 | 死区方差             | 8              | 1~64      | counts       | 噪声实测后定                                                                                                                     |
| P11 | TEMP 窗              | 0.30/0.75      | -         | V            | REF5025 手册（0.5V@25°C、1.9mV/°C，抓开路/短路）                                                                               |
| P12 | DRDY 超时            | 3              | 1~10      | ms           | 帧周期 0.256ms 的 ~12 倍                                                                                                         |
| P13 | 心跳周期             | **50**   | 20~200    | ms           | **20Hz 刷新**（用户指定）；协议 v2 §5.1                                                                                   |
| P14 | 通信超时             | **200**  | 100~1000  | ms           | **4×心跳周期**（可调）                                                                                                    |
| P15 | 亮度                 | 60             | 0~100     | %            | 默认                                                                                                                             |
| P16 | 队伍色               | 红             | 红/蓝     | -            | ID 设置时确定                                                                                                                    |
| P17 | GAIN1 档位[4]        | 1              | 1~128     | -            | 见 6.6                                                                                                                           |
| P18 | 事件使能/CRC 开关    | 开/关          | -         | -            | 联调开关                                                                                                                         |
| P19 | **超温阈值**   | **0.75** | 0.55~0.75 | V            | **默认最大值 0.75V（TEMP 窗上限 ≈157°C，等效不触发）**；REF5025 芯片温度为板温代理，热标定后收紧（1.2 保护阈值默认原则） |
| P20 | OSR 档位             | 1024           | 128~16384 | -            | 默认 1024→3.9kSPS；降档可提数据率                                                                                               |
| P21 | 环形缓冲深度 K       | 32             | 8~64      | 帧           | 预触发历史 8.2ms（6.4）；32×18B=576B RAM                                                                                        |
| P22 | 触发后窗口 W         | 32             | 8~128     | 样本         | 8.2ms 冲击衰减窗口（7.8）                                                                                                        |
| P24 | 状态上报周期         | 1000           | 100~5000  | ms           | **默认 1Hz，由裁判系统仲裁确定、可调**（9.4）                                                                              |

### 7.5 传感器损坏检测（TIM2 中断 20Hz 分频评估）

| 判据                    | 条件                                                                                                             | 故障位                           |
| ----------------------- | ---------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| 饱和/过载               | \|raw\| > 0.9×2^23 持续 1s                                                                                      | FAULT_CHx_SAT                    |
| 偏置异常（开路/桥失衡） | \|基线\| > 25%FS 持续 5s（标定后窗口）                                                                           | FAULT_CHx_OFFSET                 |
| 无响应/卡死             | 方差 <8 counts（RMS）持续 5s 且其他通道正常                                                                      | FAULT_CHx_DEAD                   |
| 时钟/SPI                | DRDY 间隔 >3ms 持续 50ms；或 SPI/CRC 错误计数突变                                                                | FAULT_SPI                        |
| 基准健康                | 4 路 REF5025 TEMP 电压出窗（0.30~0.75V，开路/短路）                                                              | FAULT_TEMP                       |
| **超温**          | 任一 REF5025 TEMP > P19 持续 5s（**P19 默认 0.75V=窗上限，等效不触发**，热标定后收紧）                     | FAULT_TEMP（**PB0 点亮**） |
| 电源                    | PVD 中断触发（当前 level 6≈2.55V；**测试阶段如误触发可临时放宽至 level 0≈1.95V**，1.2 保护阈值默认原则） | FAULT_POWER                      |

故障带迟滞与闩存：置位后需自检通过或 CAN 命令清除（每 5s 自动重试一次）。

### 7.6 自检流程（上电 + CAN 命令可触发）

1. 驱动层自检：ID/全部寄存器回读（6.3）、DRDY 翻转、NULL 帧响应。
2. 采集 100ms 统计：各通道均值（基线）、RMS 方差；1 < 方差 < 50 counts 且基线在标定窗内 → 通过。
3. TEMP 4 路在窗内（P11）且未超温（P19）。
4. **DAC 注入（可选，参数开关）**：PA4（DAC1_OUT1）输出 1.65V±0.5V 阶跃，验证注入通道响应；亦为极性校对（6.7）的注入源。
5. 结果经 STATUS_REPORT 上报；失败进入 FAULT 态但保持 CAN 通信以便诊断。

### 7.7 标定方法（M2 阶段）

- **力度两点标定（合力标定）**：19N 与 100N 砝码静压于击打平面，记录对应**合力峰值**（Σ peak_ch）→ 线性拟合 K_N；**加载点不限中心**——中心对称布局下四通道分力之和与加载位置无关。
- **通道一致性**：同一砝码逐通道加载 → K_ch[4] 归一化。
- **极性校对**：一次已知方向击打（或 DAC 注入已知符号），校验各通道响应符号 → 写入符号掩码（6.7）。
- **阈值整定**：采集 CSV 离线分析噪声分布与击打波形，THR_HIT 取"19N 标定值×1.2"或按 6σ 噪声准则取大者。
- 标定结果写入 Flash（双槽 + CRC，`calibration.c`）。

### 7.8 弹速与入射角反推（预留扩展，M6）

比赛仅一种弹丸，**不做弹丸类型分类**；弹丸质量以宏定义（值待确认，官方 17mm 弹丸约 3.2g）：

```c
#define PROJECTILE_MASS_0P1G  (32)   /* 弹丸质量，0.1g 单位，值待确认 */
```

预留"动量-撞击时间联合反推弹速与入射角"接口（`HitDetect_EstimateImpact()`，见 5.3），设计要点：

- **波形捕获**：击打触发时冻结 K 帧环形缓冲的窗口——预触发 P21 深度（≤8.2ms 历史，覆盖冲击上升沿）+ 触发后 P22 窗口（8.2ms 衰减段）→ `HitDetect_GetWaveform()` 输出完整冲击波形。
- **撞击时间（脉宽）**：超过阈值（如 THR_HIT/2）的连续样本数 × Δt（0.256ms）→ `duration_ms`。
- **动量（冲量）**：`Impulse = K_N·Δt·Σ|hp(n)|`（K_N 来自 7.7 力度标定，Σ 取波形窗口内）→ `impulse` 字段。
- **弹速估算（预留公式）**：`v ≈ Impulse / (PROJECTILE_MASS × (1+e))`，e 为恢复系数（弹丸碰撞性质修正），待 M6 实弹标定。
- **入射角估算（预留思路）**：应变桥主要测法向力——用法向冲量分量与总冲量之比结合撞击时间随角度的变化关系建模，待 M6 实弹标定。
- **击打位置估算（预留）**：四传感器中心对称分布 → 由四通道峰值分布反推击打位置：`x ∝ ((P2+P3) − (P1+P4)) / ΣP`、`y ∝ ((P1+P2) − (P3+P4)) / ΣP`（P1~P4 为 peak_ch[4]，坐标轴定义按实际布局在 M2 确定）；预留接口 `HitDetect_EstimatePosition()`，上报 TLV 预留位置字段，与弹速/入射角一同在 M6 标定落地。
- **采样率带宽说明**：3.9kSPS 下 sinc3 带宽 ≈0.26×fDATA≈1kHz，对 1~5ms 冲击脉冲基本足够；若分辨率不足，P20 OSR 降 256 → 15.6kSPS。
- **上报扩展预留**：HIT_EVENT 新增 impulse/duration TLV 字段（协议方确认后启用，见 9.4）。

---

## 8. 灯驱动设计

### 8.1 WS2812 驱动方案：USART2 2.6667MHz + DMA（已确定）

WS2812 时序要求：GRB 24bit@800kHz，0 码 0.35µs 高/0.9µs 低，1 码 0.9µs 高/0.35µs 低，周期 1.25µs±0.6µs，复位 ≥50µs。

- USART2：TX-only 异步 2.6667MHz（BRR=30，80MHz 精确）、8N1、TX DMA（Normal），DMA 完成中断置"可发送"标志。

**USART2 TX 是否需要 DMA（分析结论）**：

| 发送方式 | 开销/行为 | 结论 |
| --- | --- | --- |
| 阻塞轮询（`HAL_UART_Transmit`） | 118B@2.667MBd ≈ **阻塞主循环 442µs**/次（20Hz 刷新 ≈0.9% CPU）；阻塞期间错过 1~2 帧 ADC（K=32 环形缓冲 + drop 计数可容忍）、推迟 ISOTP 轮询 | 可用但不是最优 |
| 中断式（`HAL_UART_Transmit_IT`） | 2.667MBd 下字节间隔 375ns < 中断开销（~1-2µs/字节）→ TDR 欠载、波形破坏 | **不可行** |
| **DMA（推荐）** | 传输完全后台（404µs），完成中断置标志；仅占 1 个 DMA 通道（USART2_TX=DMA1_Ch7，与 SPI1 的 Ch2/Ch3 不冲突） | **推荐** |

**结论：DMA 不是绝对必需（阻塞式也能用），但强烈建议开**——当前 CubeMX 已启用 USART2_TX DMA（DMA1_Ch7，见第 4 章表）。
- 编码方式：每 LED 24bit → 8 字节（每字节 8 数据位 + 起始位 0 + 停止位 1 参与组帧，3 个 UART 位编码 1 个 WS2812 位：`110`=1 码、`100`=0 码）。UART 位宽 375ns → T0H 375ns∈[220,380]ns、T1H 750ns∈[700,1000]ns，均合格。
- **灯数上限 13 颗**：板载 1 颗（第 1 颗）+ 左侧装甲板 6 颗 + 右侧装甲板 6 颗，**串联顺序：板载 → 左 6 → 右 6**。帧缓冲 `led_frame[13×8+14] = 118B`，一次 DMA ≈ 404µs。
- **帧刷新 20Hz**（50ms 节拍，与 TIM2 1kHz 分频对齐；一次 DMA 404µs，刷新余量充足）：动画相位在 TIM2 1kHz 中断推进，**仅在相位变化点重算帧并启动 DMA 发送**；5Hz/1Hz 闪烁的半周期（100ms/500ms）均为 50ms 网格的整数倍，无量化失真。
- PA2 GPIO：**推挽 + Very High**（覆盖 CubeMX 半双工生成的开漏+上拉，风险 R8）。
- 亮度：全局亮度参数（P15），写入前 RGB 各通道 `v×B/100` 缩放；动画（快闪/慢闪/交替）在模式生成时按相位调幅（相位由 TIM2 1kHz 中断推进）。

### 8.2 状态联动（官方 6 语义 → 本板灯光）

| 官方语义            | 本板状态    | WS2812 行为                                        | PB1(IND_NORM=系统正常) | PB0(IND_ROHT=超温)            |
| ------------------- | ----------- | -------------------------------------------------- | ---------------------- | ----------------------------- |
| 常亮=正常（队色）   | NORMAL      | 队色常亮（红/蓝由 ID 决定）                        | **亮**           | 灭                            |
| 快闪=击打           | HIT         | 队色**5Hz 快闪**（周期 200ms，亮灭各 100ms） | **亮**           | 灭                            |
| 慢闪=ID 设置        | ID_SETUP    | 队色**1Hz 慢闪**（亮灭各 500ms）             | **亮**           | 灭                            |
| 红蓝交替=传感器损坏 | FAULT       | 红蓝 500ms 交替                                    | 灭                     | **FAULT_TEMP/超温时亮** |
| 紫常亮=通信失败     | COMM_LOST   | 紫色常亮                                           | 灭                     | 灭                            |
| 红蓝紫交替=ID 冲突  | ID_CONFLICT | 红蓝紫 333ms 循环                                  | 灭                     | 灭                            |
| （自检）            | BOOT        | 白色 300ms                                         | 灭                     | 灭                            |

注：PB0/PB1 为灌电流、**置 0 点亮**；语义已确认：PB0=温度过高指示、PB1=系统正常工作指示。

---

## 9. 通信集成设计（stm32-referee-isotp-stack）

### 9.1 协议栈结构（已调研确认）

- 传输层 `transport/isotp.c`：ISO-15765-2 标准 PCI 分帧（单帧/首帧/连续帧/流控），512B 收发缓冲，无扩展寻址、无 CRC（应用层也无 CRC，见协议 v2 说明）。
- 应用帧 `protocol/app_frame.c`：8 字节头 = dst_id、src_id、module_uid(2B LE)、func_code、seq（0=不需 ACK）、data_type、data_len + TLV 数据。
- 服务层 `service/`：按 func_code 分发（dispatcher）+ 发送队列/ACK 重试（retry_ack_scheduler，单窗口、8 条队列）。**调度器非 ISR 安全**：协议入队一律在主循环上下文执行，TIM2 中断只置标志。
- 轮询式设计（无 RTOS）：主循环调用 `Transport_Isotp_Poll()` + `Service_RetryAckScheduler_Poll()`；CAN 收经 FIFO0 中断回调。

### 9.2 移植步骤清单

1. 拷贝栈文件至 `App/comm/`（5.2 清单），transport/protocol/service **零修改**。
2. **重写 `app_can.c`**（L432 绑定）：`App_Can_GetHandle` 仅返回 `&hcan1`；滤波器 bank0 IDMASK 全接收（协议为应用层路由）；`App_Can_Init` = `HAL_CAN_Start` + `HAL_CAN_ActivateNotification(RX_FIFO0|EW|EP|BOFF|LEC)`；TX 保持"等邮箱→AddTxMessage→等 TSR 完成"逻辑（timeout 10ms）；回调桥接 `HAL_CAN_RxFifo0MsgPendingCallback → App_Can_DispatchRx`（FIFO0 排空式）；`HAL_CAN_ErrorCallback` 记日志 + Bus-Off 后 Stop/Start 自动恢复。
3. `app_log.c` 可选：USART1 115200 阻塞打印（DEBUG 开关）。
4. 实例化：`TransportIsotpContext g_isotp`（tx_id/rx_id 见 9.3，st_min_ms=10、tx_timeout=100ms、get_ms=HAL_GetTick）+ `ServiceRetryAckScheduler g_sched`。
5. 接线：`App_Can_SetRxCallback(→ Transport_Isotp_OnCanFrame)`；RX 应用帧 → `Protocol_AppFrame_Decode` → `Service_Dispatcher_DispatchRaw`；ACK 匹配 → `Service_RetryAckScheduler_OnReceivedFrame`。
6. 主循环每圈调用两个 Poll 函数。

### 9.3 CAN 层 ID 分配（**建议值，需与核心板确认**，风险 R15）

- 本板发送 ID：**0x1A0**（ISO-TP 单帧/多帧同 ID）；接收 ID：**0x2A0**；预留 0x1A1~0x1AF 给多装甲板扩展（按 armor_id 偏移）。
- 500kbps：presc=10、BS1=13、BS2=2、SJW=1、16TQ/bit、采样点 87.5%。
- 应用层地址：src_id=0x0A（装甲板，建议值）、dst_id=0x01（核心板）；module_uid=MCU UID 低 2 字节。

### 9.4 本板消息集

**上行（本板 → 核心板）**：

| 功能码 | 消息              | 数据类型                                                                     | 周期/触发                                                                   | seq/ACK                   |
| ------ | ----------------- | ---------------------------------------------------------------------------- | --------------------------------------------------------------------------- | ------------------------- |
| 0x01   | MSG_HEARTBEAT     | U32（ms 时间戳）                                                             | **50ms（20Hz**，TIM2 节拍置标志，主循环入队）                         | 0                         |
| 0x02   | MSG_STATUS_REPORT | TLV_STREAM：TLV_STATE(0x03) + TLV_VALUE(0x05, 故障位图 u16) + TLV_TEXT(0x06) | 状态变化 + 周期上报（**默认 1Hz，周期由裁判系统仲裁确定、可调 P24**） | 0                         |
| 0x31   | MSG_HIT_EVENT     | TLV_STREAM：通道(u8)+强度(u8)+力度(u16,0.1N)+时间戳(u32,ms)+峰值(u32)        | 有效击打，受 50ms 不应期约束                                                | 0（事件唯一性，禁止重试） |

**下行（核心板 → 本板）**：复用 0x51 MSG_LAUNCHER_CTRL_CMD 的 TLV_CMD_INDEX + TLV_PAYLOAD 机制（**复用该功能码与新 TLV 类型码均需协议维护者确认并同步《通信协议v2》**，风险 R15）：

| CMD_INDEX | 含义     | Payload TLV                                                                     | 应答                      |
| --------- | -------- | ------------------------------------------------------------------------------- | ------------------------- |
| 0x01      | LED 控制 | TLV_EFFECT（0=队色/1=灭/2=呼吸/3=自定义）+ TLV_VALUE(0xRRGGBB) + TLV_BRIGHTNESS | ACK                       |
| 0x02      | 参数配置 | TLV_PARAM_ID(u16) + TLV_PARAM_VALUE(u32)（写后回读校验）                        | ACK                       |
| 0x03      | 参数读取 | TLV_PARAM_ID                                                                    | 0x51 回帧 TLV_PARAM_VALUE |
| 0x04      | ID 设置  | TLV_ARMOR_ID(u8)，写入 Flash 双槽+CRC                                           | ACK（冲突→拒绝结果码）   |
| 0x05      | 自检触发 | 空                                                                              | STATUS_REPORT             |
| 0x06      | 零偏标定 | 空（当前基线固化入 Flash）                                                      | ACK                       |

新 TLV 类型码建议值（占用协议预留区，需同步更新协议文档）：0x30=HIT_CHANNEL、0x31=HIT_INTENSITY、0x32=HIT_FORCE、0x33=HIT_TS、0x34=HIT_PEAK、0x35=FAULT_FLAGS、0x36=TEMP(0.01V)、0x37=BRIGHTNESS、0x38=EFFECT、0x39=PARAM_ID、0x3A=PARAM_VALUE、0x3B=ARMOR_ID。

- 下行命令（seq≠0）→ 回复 MSG_ACK（0x06，复制 seq + TLV_ACK_FUNC），经调度器重试（3 次 × 200ms）。
- **掉线判定**：**200ms（默认 4 个心跳周期）**未收到核心板心跳 → COMM_LOST（心跳照发、状态上报）；恢复即回 NORMAL。心跳 20Hz 增加的 CAN 负载（ISOTP 单帧 8B@50ms）对 500kbps 总线可忽略（<0.3% 带宽）。

---

## 10. 任务调度（TIM2 定时中断 + 主循环）

### 10.1 设计原则

**状态维护与刷新不放在主循环阻塞跑**：开 **TIM2 1kHz 中断**以固定周期执行状态机、灯动画与健康评估节拍；主循环只承担帧消费、检测管线与协议轮询。TIM2 ISR 内只做纯计算与置标志（不调用协议栈，调度器非 ISR 安全），协议入队统一回主循环。

### 10.2 TIM2 1kHz 中断任务表（`App_OnTick1ms()`）

| 任务           | 分频 | 周期                   | 说明                                                                                              |
| -------------- | ---- | ---------------------- | ------------------------------------------------------------------------------------------------- |
| 状态机步进     | 1    | 1ms（1kHz）            | 状态转移判定与灯模式切换（第 11 章，保持不变）                                                    |
| LED 动画相位   | 1    | 1ms（1kHz）            | 快闪 5Hz/慢闪 1Hz/交替相位推进；**帧刷新输出 20Hz**（仅相位变化点重算帧并启动 DMA，见 8.1） |
| 心跳节拍       | 50   | **50ms（20Hz）** | 置标志，主循环执行协议入队                                                                        |
| 传感器健康评估 | 50   | **50ms（20Hz）** | 饱和/偏置/方差/DRDY 超时/超温（7.5）+ TEMP 窗检（ADC1 由 **TIM1 CC1 硬件触发**采集 ≈1.22kHz，本任务取最近转换结果） |
| 状态上报节拍   | P24  | 默认 1Hz               | 置标志，主循环执行协议入队；**周期由裁判系统仲裁确定、可调**                                |

ISR 耗时：状态机+相位推进 ~5µs@1kHz（0.5%）+ 健康/TEMP ~20µs@20Hz（<0.1%）+ LED 重算 ~30µs（仅相位变化点）≈ **<1% CPU**。

### 10.3 主循环任务表

| 任务              | 触发                           | 频率               | 说明                                                                                                      |
| ----------------- | ------------------------------ | ------------------ | --------------------------------------------------------------------------------------------------------- |
| 帧消费 + 检测管线 | 帧就绪标志（DMA 完成中断置位） | 3.906kHz           | 环形缓冲按读指针消费（写指针仅 ISR 改、读指针仅主循环改）；解析 18B 帧、极性应用、STATUS 检查、喂检测管线 |
| ISOTP/调度器轮询  | 每圈                           | 每圈               | `Transport_Isotp_Poll` + `Service_RetryAckScheduler_Poll`                                             |
| 心跳/状态入队     | ISR 标志                       | 50ms/默认 1Hz 可调 | 协议入队仅在此上下文                                                                                      |
| CAN RX 处理       | FIFO0 ISR                      | 事件               | ISR 内排空 FIFO，只做分发                                                                                 |

### 10.4 CPU 占用估算（80MHz）

SPI 方案 A 的 ISR 开销 ~0.8%（2µs×3.9k）+ 检测管线 ~1.2%（3µs×3.9k）+ TIM2 ISR ~1% + ISOTP/调度 ~1.5%（心跳 20Hz 流量增加）+ CAN ISR ~0.3% + 杂项 ~0.5% ≈ **总计 5~6%**，裕量充足。

**时基**：1ms 用 TIM2 中断（状态/灯/健康/心跳节拍）；HAL_GetTick（SysTick）供协议栈；HIT_EVENT 上报 ms 时间戳（u32，49.7 天回绕，可接受）。

**RAM 预算（64KB）**：协议栈 8-10KB（2×512B ISOTP 缓冲 + 调度队列 8×263B≈2.2KB + dispatcher 2KB）+ 灯缓冲 118B + **环形帧缓冲 576B（K=32×18B）** + 应用 ~2KB ≈ **13.6KB（21%）**，栈 1KB（.ioc StackSize=0x400），M0 用 map 文件复核。

---

## 11. 状态机设计

状态维护由 **TIM2 1kHz 中断每 1ms 步进**（不阻塞主循环），转移条件基于击打事件、故障位、心跳超时与下行命令。

```
  BOOT ──自检通过──▶ NORMAL ──有效击打──▶ HIT ──50ms──▶ NORMAL
    │                 │ ▲                        │
    │ 自检失败         │ │ 心跳恢复               │
    ▼                 │ │          ┌─200ms无心跳─┐ │
  FAULT ◀──故障闩存───┘ │          ▼             │ │
    │  ▲  └─自检OK─────┘        COMM_LOST       │ │
    │  │                          │ 心跳恢复      │ │
    │  │       ID_SETUP ◀──0x51 ID_SET──┐        │ │
    │  │        │ │设置完成/取消         │        │ │
    │  │        │ ▼                     │        │ │
    │  │        ID_CONFLICT ◀──主控ACK拒绝─┘       │ │
    └──重启/清错────────────────────────────────────┘
```

| 状态        | 进入条件                                   | 灯光（见 8.2）                            | 协议联动                                                  |
| ----------- | ------------------------------------------ | ----------------------------------------- | --------------------------------------------------------- |
| BOOT        | 上电                                       | 白 300ms                                  | -                                                         |
| NORMAL      | 自检通过                                   | 队色常亮，**PB1 亮**                | 心跳 50ms（20Hz）；状态变化发 STATUS_REPORT("normal")     |
| HIT         | 有效击打（peak>THR_HIT 且持续≥1.02ms）    | 队色快闪，**PB1 亮**                | 发 MSG_HIT_EVENT（seq=0）；FAULT/COMM_LOST 态不发送只计数 |
| FAULT       | 任一传感器/SPI/TEMP/超温/POWER 故障位闩存  | 红蓝交替，PB1 灭；**超温时 PB0 亮** | STATUS_REPORT("fault"+故障位图)；每 5s 自动重试自检       |
| COMM_LOST   | **200ms（4×心跳，P14 可调）**无核心板心跳 | 紫常亮，PB1 灭                            | 心跳照发；STATUS_REPORT("comm_lost")                      |
| ID_SETUP    | 收到 0x51 ID_SET                           | 慢闪，**PB1 亮**                    | 完成写 Flash → 回 ACK → 回 NORMAL                       |
| ID_CONFLICT | 主控 ACK 拒绝（重复 ID/UID）               | 红蓝紫循环，PB1 灭                        | STATUS_REPORT("id_conflict")；可重新 ID_SET               |

---

## 12. 开发里程碑

| 阶段           | 内容                                                                                                                                              | 验证方法                                                                                                                                                                                       | 周期   |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| M0 工程        | 硬件核对清单（2.3）；CubeMX 全部配置落地（第 4 章：TIM16 8MHz/TIM2 1kHz/DMA/NVIC）；app.mk 加入；编译通过                                         | `make -j`、map 文件核对 RAM/Flash；ST-Link 烧录跑马灯                                                                                                                                        | 0.5 天 |
| M1 ADC 驱动    | 第 6 章全部：TIM16 8MHz 波形、SPI Mode1 时序、复位/DRDY/全寄存器写入与回读、**方案 A（K 帧环形缓冲）DMA 帧读取**                            | 逻辑分析仪（SPI/CS/CLKIN/DRDY）；串口打印全寄存器回读值与 4 通道噪声 RMS（<50 counts）；实测 DRDY≈3906Hz；DAC 注入验证通道响应符号（极性宏验证）；环形缓冲 drop 计数为 0                      | 2-3 天 |
| M2 检测        | 第 7 章全部：CSV 离线分析 → 阈值整定 → 砝码标定（19N/100N 两点）→**极性开机击打校对落地** → 事件日志；**环形缓冲 + 波形冻结验证** | 砝码/落球台：不同力度击打，串口事件流（通道/强度/力度），示波器对比前端输出与事件峰值；20Hz 连续击打验证不应期；**DAC 注入模拟冲击校验预触发/触发后窗口完整性（HitDetect_GetWaveform）** | 2-3 天 |
| M3 灯          | 第 8 章：六状态动画（快闪 5Hz/慢闪 1Hz）+ PB0/PB1 语义联动 + 亮度；**13 灯串联（板载→左→右）**；20Hz 帧刷新                               | 目视 + 灯带；CAN 发命令切换状态；示波器测 T0H/T1H 时序裕量与 5Hz/1Hz 闪烁周期                                                                                                                  | 1 天   |
| M4 通信        | 第 9 章全量：500k 链路、ISOTP、消息集、ACK/重试、**心跳 20Hz/掉线 200ms、状态上报 1Hz 可调**                                                | CAN 回环测试（TX 接 RX）；USBCAN/candump 抓 0x1A0 帧解析（心跳 20Hz 流量）；人为拔线验证 COMM_LOST 灯与恢复；异常帧注入（乱序/流控超时）                                                       | 2-3 天 |
| M5 联调        | 核心板联调、实车/实弹、参数固化（Flash）、490N 过载验证、超温告警热标定（P19）                                                                    | 实弹连发：HIT_EVENT 频率/强度与裁判血量对比；490N 静载验证不误判饱和故障；温漂一昼夜验证基线                                                                                                   | 2-3 天 |
| M6（可选扩展） | 7.8 弹速与入射角反推：恢复系数 e 与角度模型实弹标定、`HitDetect_EstimateImpact()` 落地                                                          | 实弹不同速度/角度射击，用测速模块与高速摄影对照反推结果；精度不足时 P20 OSR 降 256 → 15.6kSPS 复验                                                                                            | 3-5 天 |

---

## 13. 风险与未决项清单

| #   | 风险/未决项                                                                                                                                                           | 影响                                | 处置                                                                      |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------- |
| R5  | **isotp 栈无 LICENSE、早期代码（2026-06）**                                                                                                                     | 法律与正确性风险                    | 车队内部使用并注明来源；M4 异常注入测试；评估后期替换或自研               |
| R6  | **RAM 预算** ~13KB/64KB（20%）、栈 1KB                                                                                                                          | 无实质风险                          | M0 map 文件复核                                                           |
| R7  | **CLKIN 8.0 vs 8.192MHz（-2.3%）**：fDATA 偏移 2.3%，数字滤波角点同移                                                                                           | 判据不受影响（按样本数/时间双标定） | M1 实测 DRDY≈3906Hz 与噪声；异常降频复验                                 |
| R8  | **USART2 半双工开漏+上拉 vs 2.667Mbps 沿速率**                                                                                                                  | WS2812 时序裕量                     | 必须改推挽+Very High；M3 示波器测 T0H/T1H                                 |
| R10 | **GAIN1/阈值依赖实弹标定**，桥灵敏度未知                                                                                                                        | 检测灵敏度偏差                      | M2 砝码两点标定；P17 逐通道增益独立配置                                   |
| R11 | **通道极性受传感器接线影响**（BIAS1_OUT 接 N 端）                                                                                                               | 符号错判                            | 极性宏（6.7）+ 开机击打校对；M1 DAC 注入验证符号后再启用检测              |
| R13 | **事件上报与官方 20Hz 语义**：50ms 不应期内两次独立击打（>20Hz 连发/异侧同时中弹）会合并为一个事件（融合模型下单次击打天然只有一个事件，无多通道重复/合并问题） | 与裁判血量计分偏差                  | M5 实弹联调核对计数语义；必要时缩小不应期+事件缓冲队列                    |
| R15 | **CAN ID（0x1A0/0x2A0）、src_id（0x0A）、0x51 复用、新 TLV 0x30~0x3B 均为建议值**                                                                               | 与核心板协议不匹配                  | 与协议维护者确认后固化到 board_comm.h 并同步《通信协议v2》                |
| R16 | **方案 B 的 CS 常低连续帧行为**手册未明引（方案 B 已确定为备选分析留存、不实现）                                                                                | 帧失步风险                          | 若未来采用方案 B，必须先做 M1 级实测验证；方案 A 不受影响                 |
| R17 | **TIM2 ISR 与主循环共享数据的一致性**（环形缓冲指针/槽状态/标志位）                                                                                             | 竞态风险                            | 写指针仅 ISR 修改、读指针仅主循环修改、槽状态用原子标志；ISR 不调用协议栈 |
| R18 | **超温阈值 P19 未经热标定**（REF5025 芯片温度为板温代理）                                                                                                       | 超温误报/漏报                       | **P19 默认 0.75V（窗上限，等效不触发）**，M5 热标定后收紧固化       |
| R19 | **弹速/入射角反推依赖波形完整性与实弹标定**（恢复系数 e、角度模型未知）                                                                                         | 反推精度不足                        | 接口预留（7.8）、算法待 M6 实弹标定验证；3.9kSPS 分辨率不足时 P20 降 OSR  |

### 13.1 未决项（需硬件/协议方确认后才能关闭）

1. CAN 层 ID 分配、src_id、0x51 复用与 HIT_EVENT 新 TLV 类型码（R15，与协议维护者同步）。
2. 通道极性标定值（M2 开机击打校对完成后固化）。
3. 超温阈值热标定（R18，默认 0.75V 不触发）。
4. `PROJECTILE_MASS_0P1G` 弹丸质量宏值（7.8，按实际弹丸确认）。
5. 状态上报周期 P24 的仲裁约定（默认 1Hz，与核心板/裁判系统确认可调范围）。
6. 击打位置估算的坐标轴定义（7.8，按实际传感器布局确定，M2）。

---

## 14. 附录：关键参数速查

| 项                         | 值                                                                                                                                                                                                       |
| -------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SYSCLK / APB1 / 定时器时钟 | 80MHz / 80MHz / 80MHz（HSE=8MHz 已确认，时钟树无需改）                                                                                                                                                   |
| SPI1                       | Mode1（CPOL=0/CPHA=1）、8bit、20MHz、软件 CS（PA15）；TX=DMA1_Ch3、RX=DMA1_Ch2                                                                                                                           |
| TIM16 CH1N                 | **8.0MHz**、50% 占空（ARR=9、CCR=5），`HAL_TIMEx_PWMN_Start` → CLKIN（HR 跑满）                                                                                                                 |
| TIM2                       | base + update interrupt**1kHz**（状态机/灯/健康节拍），NVIC 优先级 3                                                                                                                               |
| ADS131M04                  | CLOCK=0F0Eh（4CH 使能+OSR=1024+PWR=HR）、MODE=0510h→0410h（24bit/CRC 关/清 RESET 标志）、GAIN1 初始=1；**全部可写寄存器显式写入+回读校验**；帧 6 字 18B；**fDATA≈3906Hz**（帧周期 256µs） |
| 帧读取                     | **方案 A（已确认采用）：DRDY EXTI → TX+RX DMA → K=32 环形缓冲（576B，预触发 8.2ms）**；方案 B（CS 常低+TIM2 周期 DMA）为备选分析留存                                                             |
| CAN1                       | 500kbps：presc=10、BS1=13、BS2=2、SJW=1、采样点 87.5%；FIFO0 中断收、邮箱发；TX ID 0x1A0 / RX ID 0x2A0（建议值）                                                                                         |
| USART2                     | 2.6667MHz（BRR=30）、8N1、TX-only、DMA，WS2812（8B/LED，**≤13 颗：板载→左 6→右 6**）；**帧刷新 20Hz**                                                                                     |
| USART1                     | 115200 调试日志（可选）                                                                                                                                                                                  |
| ADC1                       | **4 通道扫描 IN6/IN10/IN11/IN12 + TIM1 CC1 硬件触发（≈1.22kHz），SamplingTime=47.5 周期**（TEMP/超温，窗检并入 20Hz 健康评估）                                                                     |
| DAC1                       | OUT1（PA4）自检注入/极性校对激励                                                                                                                                                                         |
| 检测                       | 基线 EMA α=2^-13（τ≈2.1s）、窗口 32 样本（8.2ms）、持续≥4 样本（1.02ms）、不应期 50ms（≤20Hz）                                                                                                      |
| 极性                       | `ADC_CH_SIGN_MASK` 宏（bit0-3=ch0-3）+ `HitDetect_CalibratePolarity()` 开机击打校对接口                                                                                                              |
| 灯光                       | **快闪 5Hz（周期 200ms）、慢闪 1Hz**；帧刷新 20Hz（50ms 节拍）                                                                                                                                     |
| 通信                       | **心跳 50ms（20Hz）**、**掉线判据 200ms（4×心跳）**、**状态上报默认 1Hz 可调（P24，仲裁确定）**、HIT_EVENT seq=0、命令 ACK 3 次×200ms 重试                                           |
| 弹丸反推（预留）           | `PROJECTILE_MASS_0P1G` 宏（0.1g 单位，值待确认）；冲量 = K_N·Δt·Σ\|hp\|；`HitDetect_EstimateImpact()` 弹速/入射角反推接口（M6）                                                                  |
| CPU                        | 总负载 ~5-6%（80MHz；SPI ISR ~0.8%、检测 ~1.2%、TIM2 ISR ~1%）                                                                                                                                           |
| RAM                        | 应用预算 ~13.6KB / 64KB（协议栈 8-10KB 为主；灯缓冲 118B + 环形帧缓冲 576B）                                                                                                                             |

### 关键文件索引

| 文件                                                             | 角色                                                                                    |
| ---------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| `Armor_Cmzy26_081.ioc`                                         | **引脚/外设定义的权威来源**；第 4 章全部 CubeMX 配置修改的落点                    |
| `Core/Src/main.c`                                              | USER CODE 块内接入 App_Init/App_Loop/App_OnTick1ms 与中断回调（唯一允许手改的生成文件） |
| `Driver_Examples/ads131m08/ads131m0x.h`                        | ADS131M0x 寄存器位定义权威来源（App/bsp/ads131m04.h 的移植基准）                        |
| `Driver_Examples/stm32-referee-isotp-stack/Core/Src/app_can.c` | 需为 L432 重写的 CAN 桥接层（栈其余文件零修改拷贝）                                     |
| `Makefile`                                                     | 末尾追加`-include app.mk`（CubeMX 重生成后需恢复）                                    |

---

*本文档基于 CubeMX 配置、RM0394、ADS131M04 数据手册与裁判系统文档调研编写；标注"建议值/待确认"的参数须经硬件与协议方确认后固化。*
